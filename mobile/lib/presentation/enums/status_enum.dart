enum Status { loading, loaded, failure, success }

enum SignInStatus {
  loading,
  loaded,
  failure,
  toClientMainScreen,
  toCoachMainScreen,
}

enum InitialStatus {
  loading,
  toOnBoardingScreen,
  toClientMainScreen,
  toCoachMainScreen,
}
